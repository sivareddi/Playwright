let users = [
    "admin",
    "manager",
    "tester",
    "developer",
    "guest"
];

for (let usersCounter = 0; usersCounter < users.length; usersCounter++ )
{
    console.log("Executing Login Test for " + users[usersCounter]);
}