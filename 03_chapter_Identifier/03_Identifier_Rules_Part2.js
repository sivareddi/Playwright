var name = "Reddy";
console.log(name); // Reddy
var firstName = "Siva";
console.log(firstName);
var lastName = "Reddy";
console.log(lastName);
var fullName = firstName + " " + lastName;
console.log(fullName); // Siva Reddy
let userName = "camelCase";
console.log(userName);
let totalPrice = 99.99;
console.log(totalPrice);
let isLoggedIn = true;
console.log(isLoggedIn);
// This is PascalCase generally used for class names
let UserProfile = "PascalCase"; // 
console.log(UserProfile);

let ShoppingCart = "Class name style"; //
console.log(ShoppingCart);

//3. snake_case - generally used for file names in Python, Ruby, and other languages
let user_name = "snake_case"; // use underscores to separate words in the identifier
console.log(user_name);

//4. SCREAMING_SNAKE_CASE - generally used for constants in many programming languages

const MAX_VALUE = 100; // use all uppercase letters and underscores to separate words in the identifier
console.log(MAX_VALUE);
const API_KEY = "abc123"; // use all uppercase letters and underscores to separate words in the identifier
console.log(API_KEY);
const DATABASE_URL = "localhost:5432"; // use all uppercase letters and underscores to separate words in the identifier

//5. Hungarian Notation - generally used for variable names in some programming languagess
let strName = "Hungarian Notation"; // use a prefix to indicate the type of the variable, followed by the variable name
console.log(strName);
let bActive = true; // use a prefix to indicate the type of the variable, followed by the variable name
console.log(bActive);
let nCount = 10; // use a prefix to indicate the type of the variable, followed by the variable name
console.log(nCount);
